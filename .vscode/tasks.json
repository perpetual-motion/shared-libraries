{
  "version": "2.0.0",
  "tasks": [{
      "label": "Watch",
      "type": "shell",
      "options": {
        "cwd": "${workspaceFolder}"
      }  ,

      "command": "common/temp/node_modules/.bin/tsc --build ${workspaceFolder}/tsconfig.projects.json --watch",
      "isBackground": true,
      "problemMatcher": "$tsc-watch",
      "group": {
        "kind": "build",
          "isDefault": true
      }
    }
  ]
}
